import { css } from 'emotion';

export const sideBarContainer = css`
    display: flex;
    flex-direction: column;
    justify-content: center;
    width: 200px
    height: 100vh;
    position:fixed;
    top:0;
    left:0;
`